apiVersion: v1
kind: Service
metadata:
  name: broker
  namespace: tod
  labels:
    app: broker-rds
spec:
  ports:
    - port: 1883
      targetPort: 1883
      nodePort: 31883
  selector:
    app: broker-rds
  type: NodePort
