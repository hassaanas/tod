apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-tod-code
  namespace: tod
spec:
  storageClassName: local-storage
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/home/ubuntu/tod/tod-code"
